from setuptools import setup, find_packages

setup(
    name = "src", 
    version = "0.0.1",
    description = "its MLOPS on WineQ",
    author = "Rajat",
    packages = find_packages(),
    license = "MIT",
    
)